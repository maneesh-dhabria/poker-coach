# Verification Review — ux-ui-cleanup

**Date:** 2026-06-03 · **Branch:** `feat/ux-ui-cleanup` · **Tier:** 2 (Enhancement) · **Mode:** interactive
**Spec:** `02_spec.md` (14 FRs, 6 edge cases) · **Result:** ✅ **PASS**

## 1. Static Verification

| Check | Command | Result |
|---|---|---|
| Lint | `npm run lint` | ✅ exit 0 — No issues found |
| Types | `npm run typecheck` | ✅ exit 0 |
| Unit/component tests | `npm test` | ✅ **256 passed (45 files)** |
| Production build | `npm run build` | ✅ Compiled successfully, 8/8 static pages |

(Re-run after the one verification fix below — still 256/256, lint 0, typecheck 0.)

## 2. Code-Quality Review (multi-angle subagent)

**No blocking issues (nothing ≥ 75).** `selectActingSeat()` confirmed behavior-preserving vs. the
old inline gate, plus the new during-reveal glow branch. Repo-wide grep found **no stale `TabKey`
references** (`feedback`/`hands`/`rankings`/`preflop`). `core/*` untouched — purity rule intact.
Components stay presentational; FeedbackPanel still reads `DecisionAnalysis` without recomputing.

**Verification fix applied:** the `RightPanel.tsx` header comment still described the old "Hands"
panel and "filled in W4/W5" — comment-rot introduced by this consolidation. Updated to describe the
three-tab layout. (Commit below. No behavioral change → no regression test required.)

Non-blocking (info only, <75): `RightPanel.tsx:39` calls `flow.tableView()` once in the
live-feedback branch — negligible, pre-existing pattern; not changed.

## 3. Interactive QA (Playwright MCP, localhost:3001)

The four items /execute deferred as jsdom-unassertable were verified live in the browser.

### Acting-seat glow walk (FR-12/13/14, G4, E3, E5)
Dealt hands and sampled `.acting-glow` over time while keeping the action flowing:

```
t=568   Bot 1 MP   (revealing)
t=975   Bot 2 CO   (revealing)   ~407ms
t=1333  Bot 3 BTN  (revealing)   ~358ms
t=1741  Bot 4 SB   (revealing)   ~408ms   ≈ REVEAL_MS 380 → E5 pacing confirmed
t=2098  Bot 5 BB   (revealing)
 ...    (walks bot seats across betting rounds — 20 distinct bot states, 147 samples)
t=6736  You BB     (revealing:false)   → glow rests on the hero on the hero's turn
```

- **`maxGlowCount = 1`** for every sample — exactly one seat glows; never 0 mid-action, never 2+.
- Post-reveal glow lands on the hero with the action bar showing; on hand-over nothing glows.
- **E3 reduced-motion:** the static gold ring is a `box-shadow`
  (`rgb(216,177,90) 0 0 0 2px, …`) on `.acting-glow`; the pulse animation is gated behind
  `@media (prefers-reduced-motion: no-preference)`. Under `reduce` only the static ring shows — for
  every acting seat, since the glow is class-driven (bot seats inherit it unchanged).

### Tabs & panels (FR-01..08, FR-09)
- Exactly **3 tabs**: `Live Feedback` (default-selected), `Coaching`, `References`.
- **No "Hand review" rendered below the table** (`handReviewOutsideTabBody: []`) — FR-09 / G2.
- **References** stacks `Hand rankings` + `Preflop chart` in one scroll region (FR-06).
- **Live Feedback** is the default on deal (FR-01); keyboard roving-focus intact (FR-04).

### Coaching markdown styling (FR-10/11, E2, D5)
Coaching tab showed the empty state (E2 — `.coaching-doc` rules correctly do not touch it). A
synthetic `.coaching-doc` article injected against the live stylesheet produced the intended
computed hierarchy, and an unclassed control `<h1>` stayed at 32px default — **proving the rules are
scoped and do not bleed** (D5):

| Element | Computed |
|---|---|
| `.coaching-doc h1` | 22px / 700 / gold / margin-top 20px |
| `.coaching-doc h2` | 18px / 700 / ink |
| `.coaching-doc h3` | 15px / 600 / UPPERCASE / letter-spacing 0.6px |
| `.coaching-doc p` | line-height 25.6px (1.6) / margin 8px |
| `.coaching-doc ul` | padding-left 20px |
| `.coaching-doc strong` | gold / 700 |
| control `h1` (no class) | **32px default — unstyled** |

### Stale-tab / hard-reload (FR-02, E4, P7/P8)
Hard reload returns cleanly to the **New Session** screen (session is ephemeral; `activeTab` is not
persisted) — no crash, no blank panel; a fresh deal defaults to `live-feedback`. The `setActiveTab`
coercion-to-default guard (FR-02/D7) is the defensive backstop and is unit-tested
(`RightPanel.test.tsx` stale-key `@ts-expect-error` case).

**P9 console:** the only console error is a `favicon.ico` 404 — pre-existing dev artifact, not
introduced by this feature, not user-facing.

Evidence: `verify/2026-06-03-evidence/play-shell.png`.

## 4. Spec Compliance — all 14 FRs

| ID | Requirement | Outcome | Evidence |
|----|-------------|---------|----------|
| FR-01 | `TabKey` 3-union, default `live-feedback` | Verified | `sessionStore.ts:10,53`; live default-selected tab |
| FR-02 | `setActiveTab` coerces unknown → default | Verified | `sessionStore.ts:12,57`; `RightPanel.test.tsx` coercion test |
| FR-03 | `TABS` = 3 tabs in order | Verified | `TabStrip.tsx:7-11`; live 3-tab strip |
| FR-04 | Keyboard nav over 3 tabs unchanged | Verified | `TabStrip.tsx:20-40` (reads TABS generically); `RightPanel.test.tsx` |
| FR-05 | Live Feedback stacks FeedbackPanel + HandRecap | Verified | `RightPanel.tsx:35-42`; `RightPanel.test.tsx` |
| FR-06 | References stacks Rankings + PreflopChart | Verified | `RightPanel.tsx:44-49`; live "Hand rankings" + "Preflop chart" |
| FR-07 | Coaching branch unchanged but for class | Verified | `RightPanel.tsx:43` |
| FR-08 | Old tab branches removed, no component deleted | Verified | grep: no stale TabKey refs; all 6 imports retained |
| FR-09 | Remove `<HandRecap>` below the table | Verified | grep PokerTable.tsx → 0 HandRecap; live `handReviewOutsideTabBody: []` |
| FR-10 | `className="coaching-doc"` on `<article>` | Verified | `CoachingViewer.tsx:123`; `CoachingViewer.test.tsx` |
| FR-11 | Scoped `.coaching-doc` typography from tokens | Verified | `globals.css:209-219`; live computed-style table (§3) |
| FR-12 | `actingSeat` reveal-aware selector | Verified | `PokerTable.tsx:21-29,58`; `PokerTable.glow.test.tsx`; live walk |
| FR-13 | `isActing={s.seat === actingSeat}` | Verified | `PokerTable.tsx:121`; live `maxGlowCount=1`, walks bots |
| FR-14 | Glow respects reduced-motion (inherited) | Verified | `globals.css` no-preference gate; live box-shadow + gated pulse |

### Edge cases

| # | Scenario | Outcome | Evidence |
|---|----------|---------|----------|
| E1 | No hand yet | Verified | FeedbackPanel empty + HandRecap null (`RightPanel.tsx:38`) |
| E2 | Coaching empty | Verified | Live "No coaching yet" empty state; `.coaching-doc` not applied |
| E3 | Reduced motion | Verified | Static ring box-shadow + pulse gated behind no-preference |
| E4 | Stale persisted tab | Verified | FR-02 coercion (unit-tested); hard-reload → clean default |
| E5 | Fast bot sequence | Verified | Live walk at ~358–408ms/action (≈ REVEAL_MS 380) |
| E6 | Hero acts first | Verified | Live: hero-UTG hand glowed hero immediately (post-reveal branch) |

## 5. Plan Compliance

| Task | Outcome | Evidence |
|------|---------|----------|
| T1 (tabs merge) | Verified-complete | `a70751f` + RightPanel/TabStrip/sessionStore tests |
| T2 (remove duplicate HandRecap) | Verified-complete | `bb65f28` + `PokerTable.handrecap.test.tsx` + live |
| T3 (coaching styling) | Verified-complete | `9b1c866` + `CoachingViewer.test.tsx` + live computed styles |
| T4 (glow follows actor) | Verified-complete | `111e876` + `PokerTable.glow.test.tsx` + live walk |
| TN (final verification) | Verified-complete | `task-0N.md`; gate green |

## 6. Gaps

**None.** No critical/medium/low gaps. One comment-rot fix applied during verification (§2).

## 7. Phase Gates

- **Phase 6 hardening:** no behavioral bug found during verify → no new regression test needed; the
  reveal-aware selector, source-removal guard, and coercion are already unit-asserted.
- **Phase 7 comments-coverage hard gate:** NA — that gate targets the pmos-toolkit repo
  (`scripts/check-comments-coverage.sh` absent here); not applicable to this app.
- **Phase 7.5 design-drift:** skipped — no `DESIGN.md` / wireframes in this feature.

## Outcome

**PASS** — 14/14 FRs Verified, 6/6 edge cases Verified, all gates green, no open items.
