# Verification Review — UX & Learning Overhaul

**Feature:** ux-learning-overhaul
**Branch:** feat/ux-learning-overhaul
**Date:** 2026-05-31
**Spec:** `docs/pmos/features/2026-05-31_ux-learning-overhaul/02_spec.md`
**Mode:** interactive
**Run Outcome:** pass (all FRs Verified or NA-alt-evidence; live no-scroll confirmed via Playwright @1280×800)
**Open Questions:** 0

---

## Summary

| Phase | Result |
|---|---|
| 2 — Static (lint / types / tests) | ✅ typecheck rc=0, lint 0 issues, **vitest 39 files / 195 tests pass** |
| 3 — Code-quality / architecture review | ✅ all 7 architectural assertions hold |
| 4 — Integration / UI verification | ✅ component tests + source + **live Playwright @1280×800** (no-scroll confirmed) |
| 5 — Spec compliance (FR/NFR/edge) | ✅ every FR Verified or NA-alt-evidence |
| 6 — Test hardening | ✅ each new surface ships with its own tests (TDD per-task); no new gaps found |
| 7 — Final pass | ✅ no runtime debug code; one build-time `console.log` in the generator script (intentional) |

---

## Phase 2 — Static Verification (evidence)

```
npm run typecheck   → tsc --noEmit, exit 0
npm run lint        → ESLint: No issues found, exit 0
npm test            → Test Files  39 passed (39)
                      Tests      195 passed (195)
```

## Phase 3 — Architectural assertions (§17 of spec)

| Assertion | Result | Evidence |
|---|---|---|
| `core/*` no React/DOM/app/components/store imports | ✅ CLEAN | grep over `core/**/*.ts` (excl tests) — no matches |
| `core/*` no filesystem IO | ✅ CLEAN | grep `fs`/`path` imports in `core/` — none |
| No runtime LLM / Anthropic SDK | ✅ CLEAN | grep `@anthropic`/`new Anthropic` across `core,components,store,app,lib` — none |
| `components/*` no inline $/BB conversion | ✅ CLEAN | grep `/bigBlind`/`/bb` outside `money.ts` — none |
| `core/analysis/analyze.ts` unchanged (FR-61) | ✅ UNCHANGED | `git diff main...HEAD -- core/analysis/analyze.ts` empty |
| `HANDRECORD_SCHEMA_VERSION = 1` (NFR-06) | ✅ =1 | `core/history/handRecord.ts:8`; file unchanged on branch |
| LEFT column non-scrolling; only TabBody scrolls (FR-03) | ✅ | `page.noscroll.test.tsx` (3 passing tests assert the CSS contract) |

## Phase 5 — Spec Compliance

### Functional Requirements

| ID | Outcome | Evidence |
|---|---|---|
| FR-01 grid/100vh/left non-scroll | Verified | `app/page.tsx` + `app/globals.css` contain `100vh` (2 files); `page.noscroll.test.tsx::"left column is a non-scrolling flex column filling 100vh"` |
| FR-02 Header+Table+ActionBar left col | Verified | `page.noscroll.test.tsx`; T1/T2 commits |
| FR-03 only TabBody overflow-y:auto | Verified | `page.noscroll.test.tsx::"only the tab body is a scroll region"` |
| FR-04 5 tabs, activeTab in sessionStore | Verified | `RightPanel.test.tsx`; `store/sessionStore.ts` (`activeTab`) |
| FR-05 setup reflow no-scroll | Verified | `page.noscroll.test.tsx::"setup screen wrapper opts into no-scroll layout"` |
| FR-06 scrollHeight ≤ innerHeight @1280×800 | Verified | **Live Playwright @1280×800 on the running dev server:** `document.documentElement.scrollHeight = 800`, `window.innerHeight = 800` → `noScroll: true` on the setup screen. Backed by the structural CSS-contract test `page.noscroll.test.tsx` (the D4 automated guard). In-hand fold not re-driven this run (would require dealing a hand via the UI), but the shell that enforces no-scroll is shared between setup and in-hand (`app/page.tsx` `height:100vh; overflow:hidden`, sole scroll region `#tab-body`). |
| FR-07 existing content moved to tabs, no behavior change | Verified | `RightPanel.test.tsx`; reused FeedbackPanel/CoachingViewer/HandRecap |
| FR-10 acting "thinking" glow | Verified | `Seat.test.tsx::"marks the acting seat with a thinking glow"` |
| FR-11 `handCategoryLabel` pure | Verified | `core/eval/handEval.test.ts` (per-category incl. "Two Pair, Aces & Kings") |
| FR-12 `winningCards` pure best-5 | Verified | `core/eval/handEval.test.ts` (exact best 5 incl. wheel/flush) |
| FR-13 winner glow + yellow cards + banner | Verified | `Seat.test.tsx::"highlights winning cards"` + `CenterStack.test.tsx::"shows the hand-category banner at showdown"` |
| FR-14 per-seat net chip +$/−$ | Verified | `Seat.test.tsx::"renders a per-hand net chip with sign + color"` |
| FR-15 fold-out: net only, no banner/highlight | Verified | `CenterStack.test.tsx::"hides the banner on a fold-out (no showdown)"` |
| FR-16 split pots: multi glow + shares | Verified | `Seat.test.tsx` winner glow + `core/handFlow.test.ts` winners[] |
| FR-20 bankroll.json shape | Verified | `core/bankroll.test.ts` (defaultBankroll shape) + `route.test.ts` |
| FR-21 GET/PUT /api/bankroll | Verified | `route.test.ts::"PUT then GET round-trips the bankroll"` |
| FR-22 stacks carry hand-to-hand | Verified | `bankrollStore.test.ts` + `core/bankroll.test.ts` (carry-over) |
| FR-23 lifetime bank persists | Verified | `route.test.ts` round-trip + `lib/dataStore.test.ts` (atomic write) |
| FR-24 bots auto-rebuy | Verified | `core/bankroll.test.ts` (auto-rebuy) + `bankrollStore.ts` (`rebuy` x4) |
| FR-25 hero rebuy modal | Verified | `RebuyModal.test.tsx::"offers a top-up to the starting stack…"` |
| FR-26 New table resets stacks+pnl, keeps bank | Verified | `core/bankroll.test.ts` (newTable) |
| FR-27 stack presets 50/100/200, default 100 | Verified | `HeaderBar` presets + `bankroll.test.ts` |
| FR-28 header P/L (▲/▼) + bank via formatMoney | Verified | `HeaderBar.test.tsx` (up/down glyph + bank via formatMoney) |
| FR-29 empty-bank end state, never dead table | Verified | `RebuyModal.test.tsx::"offers New table / out-of-chips when the bank is empty"` |
| FR-30 corrupt/missing → default, no crash | Verified | `route.test.ts::"GET returns a fresh default when the file is corrupt"` |
| FR-31 pure bankroll reducer | Verified | `core/bankroll.test.ts`; core purity grep clean |
| FR-40 9 categories strongest-first | Verified | `RankingsTab.test.tsx::"lists all nine hand categories strongest-first…"` |
| FR-41 derives from HandCategory enum | Verified | `RankingsTab.test.tsx::"derives rows from the HandCategory enum (single source)"` |
| FR-50 13×13 / 169-hand grid | Verified | `PreflopChartTab.test.tsx::"renders 169 cells…"` + `preflop.test.ts` (allHands169) |
| FR-51 cells colored by chartAction (no split) | Verified | `core/charts/preflop.test.ts` (grid color mapping) |
| FR-52 cells = keyboard `<button>` + aria-label | Verified | `PreflopChartTab.test.tsx::"renders 169 cells as keyboard-reachable buttons with aria-labels"` |
| FR-53 position selector defaults to hero pos | Verified | `PreflopChartTab.test.tsx::"defaults the position selector and lets you change positions"` |
| FR-54 detail card: equity + plain defs + caveat | Verified | `PreflopChartTab.test.tsx::"shows a detail card with equity + plain definitions…"` |
| FR-55 equity from precomputed JSON (169 keys) | Verified | `node` count of `preflopEquity.json` → **169 keys** |
| FR-56 missing-key fallback, no LLM | Verified | `PreflopChartTab.tsx` fallback path (`equityResultSync`/"—") + `preflop.test.ts` fallback |
| FR-60 no banned jargon ("price to continue") | Verified | grep across `core/components/app` → banned phrase only in `explain.test.ts` as the *absence* assertion |
| FR-61 terms defined inline; analyze.ts unchanged | Verified | `explain.test.ts` (inline-definition guard); `analyze.ts` diff empty |
| FR-62 winner narration on fold | Verified | `explain.test.ts` (narrateWinner from fixture OutcomeRecord) |
| FR-63 honors gtoClaim ("baseline" preflop only) | Verified | `explain.test.ts` (gtoClaim gating) |
| FR-64 mucked winner → degrade, no invented hand | Verified | `explain.test.ts` (shown-cards-absent case) |
| FR-70 `formatMoney` usd/bb pure | Verified | `core/money.test.ts` |
| FR-71 displayUnit toggle on hero stack | Verified | `Seat.test.tsx::"toggles hero stack between dollars and big blinds on click"`; `sessionStore.ts` displayUnit |
| FR-72 all money via formatMoney; engine in $ | Verified | no inline conversion grep clean; `CenterStack.test.tsx` (pot via formatMoney), `HeaderBar.test.tsx` |

### Non-Functional Requirements

| ID | Outcome | Evidence |
|---|---|---|
| NFR-01 zero scroll ≥1280×800 | Verified | live Playwright @1280×800 (scrollHeight 800 ≤ 800) + `page.noscroll.test.tsx` |
| NFR-02 core purity + unit-tested | Verified | purity grep clean; money/bankroll/handEval/explain all have `.test.ts` |
| NFR-03 no runtime LLM; equity precomputed | Verified | LLM grep clean; `preflopEquity.json` 169 keys |
| NFR-04 corrupt bankroll never crashes | Verified | `route.test.ts` corrupt-file → default |
| NFR-05 a11y: keyboard buttons + aria-labels | Verified | `PreflopChartTab.test.tsx` (buttons + aria-labels); sign/glyph color cues in `Seat`/`HeaderBar` |
| NFR-06 HandRecord stays v1; existing tests green | Verified | schema =1, file unchanged; 195/195 incl. pre-existing |
| NFR-07 preflop detail opens instantly | Verified | precomputed-lookup path (no MC on click); 169-key table committed |

### Edge cases

| # | Outcome | Evidence |
|---|---|---|
| E1 bank empty on bust | Verified | `RebuyModal.test.tsx` empty-bank end state |
| E2 corrupt bankroll file | Verified | `route.test.ts` corrupt → default |
| E3 missing precomputed equity | Verified | `PreflopChartTab` fallback + `preflop.test.ts` |
| E4 fold-out no showdown | Verified | `CenterStack.test.tsx` hides banner |
| E5 split pot | Verified | `handFlow.test.ts` winners[] + Seat glow |
| E6 narrow viewport waiver | NA — alt-evidence | desktop-first scope (Non-Goal); no <1280 assertion required |
| E7 winner mucked | Verified | `explain.test.ts` shown-absent degrade |
| E8 BB toggle precision | Verified | `core/money.test.ts` (rounding ≤1 decimal) |

## Phase 4c — Plan task compliance

All 19 tasks (T1–T19) + TN present in `03_plan.md` and implemented; each landed with its own tests (per-task TDD cadence). Spot evidence: T17 PreflopChartTab (efc-series commit), T18 verdict reword + T19 narrateWinner (`explain.ts` + `explain.test.ts`). No task is claimed-but-unverified.

## Phase 7 — Final pass

- No runtime debug code (`console.log`/`debugger`/`TODO`/`FIXME`/`HACK`) in shipped `core`/`components`/`store`/`app`/`lib`.
- One intentional `console.log` in `scripts/genPreflopEquity.ts:62` — a build-time generator that reports how many hands it wrote; not on any runtime path.
- Live-run console: only a `favicon.ico` 404 (cosmetic) and an HMR-WebSocket disconnect after the dev server was stopped — no application errors.
- `core/charts/preflopEquity.json` is committed generated output (read-only at runtime), 169 keys.

---

## Open / follow-up items

None blocking. Optional cosmetic: add a `favicon.ico` (or a `<link rel="icon">`) to silence the dev-time 404 — out of scope for this feature.

No code changes were required during verification — the implementation was already green across static, architectural, per-FR test, and live-run evidence.
