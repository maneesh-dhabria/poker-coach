# ⚠️ NOT cleared for sharing until you complete this checklist.

**This playbook is NOT safe to post.** `/playbook` flags; it never auto-scrubs and never marks content "safe." You are the share gate. Nothing in the pipeline has cleared this file — only you can.

This is a **personal** repo (`poker-coach`), so most flags below are low-risk. Confirm anyway.

---

## 1. Flagged text — decide Keep or Redact for each

| Item | Type | Where | Keep / Redact? |
|---|---|---|---|
| "Claude" / Claude Code | Product name | What this is; throughout | ☐ |
| "Playwright" | Product name (tool) | v0.3; v0.4 | ☐ |
| "Monte Carlo" | Method name | v0.4; process | ☐ |
| pmos-learnkit, `/feature-sdlc`, `/grill`, `/spec`, `/verify`, `/requirements`, `/plan`, `/execute` | Skill/plugin names | Throughout | ☐ |
| `v0.2` / `v0.3` / `v0.4` / `v0.5` | Version numbers (app's own) | Throughout | ☐ |
| `$20`, `$60` | $-amounts (illustrative poker, NOT business metrics) | v0.3 excerpt | ☐ |
| `25%`, `40%` | Percentages (poker equity, NOT KPIs) | v0.3 excerpt | ☐ |
| `docs/mental-equity-guide.md`, `core/...` paths | Repo-relative file paths (generic, no usernames) | v0.4; v0.2 | ☐ |
| Footer link to pmos-learnkit README (GitHub) | URL (public, intentional attribution) | Page footer | ☐ |

**No personal identifiers** (real name, email, GitHub username, repo SSH URL, private hostnames, API keys) were written into the article prose or tweet thread — verified by scan. If you add any, re-check.

## 2. Screenshots — eyeball every image (cannot be text-scanned)

- ☐ `screenshots/v0.3-play-screen.png` — I have eyeballed this image for sensitive content. *(Wireframe of the play screen + coaching panel; sample hand data only — no real account, name, or balance.)*
- ☐ `screenshots/v0.4-mental-math.png` — I have eyeballed this image for sensitive content. *(Wireframe of the Mental Math walk-through; sample hand data only.)*

## 3. Sign-off

- ☐ Reviewed; cleared to share.
